name: vhelp_test
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  syncfusion_flutter_calendar: ^19.2.55
  cupertino_icons: ^1.0.2
  splashscreen: any
  form_field_validator: ^1.1.0
  firebase_core: ^1.4.0
  firebase_auth: ^3.0.1
  fluttertoast: ^8.0.8
 # device_calendar: ^3.1.0
  # database storage
  sqflite: ^2.0.0+3
  # format date time
  intl: ^0.17.0
   # display todos in grid
  flutter_staggered_grid_view: ^0.4.0-nullsafety.3
  flutter_local_notifications: ^8.1.1+1
  flutter_emoji: ^2.2.1+1
  connectivity: ^3.0.6
  provider: ^6.0.0
  url_launcher: ^6.0.9
  cloud_firestore: ^2.5.1
  shared_preferences: ^2.0.7
  logger: ^1.1.0
  path_provider: ^2.0.5
  #http for podcast and music
  http: ^0.13.4
  rxdart: ^0.27.1
  flutter_native_timezone: ^2.0.0
  youtube_player_flutter: ^8.0.0
  cached_network_image: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  image_path: "assets/images/iceberg.png"
  android: true
  ios: true

flutter:
  uses-material-design: true
  assets:
  - assets/images/
